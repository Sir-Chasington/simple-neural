{
  "name": "simple-neural",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "author": "Chase Lester",
  "license": "ISC",
  "scripts": {
    "start": "parcel src/index.html -d tmp --open",
    "build": "parcel build ./src/index.html -o simple-neural.html -d build",
    "watch": "yarn build && parcel watch ./src/index.js -d tmp"
  },
  "dependencies": {
    "@babel/runtime": "^7.7.4",
    "ramda": "^0.26.1"
  },
  "devDependencies": {
    "@babel/core": "^7.7.4",
    "@babel/plugin-transform-runtime": "^7.7.4",
    "parcel-bundler": "^1.6.2"
  }
}
